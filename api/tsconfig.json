{
  "//": "The root tsconfig.json targets the Vite frontend: it sets noEmit, allowImportingTsExtensions, moduleResolution \"bundler\" and JSX. Those settings are wrong for — and can outright break — the TypeScript compile the platform runs for a Node serverless function, so the function gets its own Node-shaped configuration here.",
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022"],
    "module": "CommonJS",
    "moduleResolution": "Node",
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "resolveJsonModule": true,
    "skipLibCheck": true,
    "noEmit": false,
    "types": ["node"]
  },
  "include": ["./**/*.ts", "../server.ts", "../src/server/**/*.ts"]
}
